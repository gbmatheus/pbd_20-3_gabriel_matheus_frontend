import styled from 'styled-components';

const HeaderEniatus = styled.header `
  padding: 10px 20px;
  background-color: var(--color-background);
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-bottom: 2px solid var(--color-in-background);
`;

export default HeaderEniatus;